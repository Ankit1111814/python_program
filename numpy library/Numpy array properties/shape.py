# shape for find the row and column of the array

import numpy as np
arr= np.array([[2,4,3],[3,2,1],[1,2,3]]) # Creates a 3D array of shape (2, 4, 3) filled with ones
print(arr.shape) # Prints the shape of the array
print(arr) # Displays the array filled with ones