# dtype find the data type of the array
import numpy as np
arr = np.array([[2, 4, 3], [3, 2, 1], [1, 2, 3]])  # Creates a 2D array
print(arr.dtype)  # Prints the data type of the elements in the array
print(arr)  # Displays the array