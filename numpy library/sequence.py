# np.arrange(start, stop, step) creates an array with a range of values
# start (0) default start value, stop (10) exclusive end value, step (1) default step value

import numpy as np
arr= np.arange(0, 10, 1)  # Creates a 1D array with values from 0 to 9
print(arr)